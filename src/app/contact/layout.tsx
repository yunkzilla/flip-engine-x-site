import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Us — Flip Engine X | FBA Scanning & Sourcing App",
  description: "Get in touch with the Flip Engine X team. Questions about our FBA scanning app, pricing, enterprise plans, or partnership opportunities? We'd love to hear from you.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
